# Sprint Plan Dashboard

This repository contains the Sprint planning Gantt dashboard.

## Structure
- `preview.html` – main dashboard (share this with the team)
- `app.js` – client logic for timeline editing and collaboration
- `docs/supabase-collaboration.md` – shared database rollout plan
- `supabase/schema.sql` – database schema and starter RLS policies

## Branches
- `main` → stable version for team use
- `experiment/improvements` → for testing and iteration

## Usage
Open `preview.html` in a browser.

## Realtime Collaboration

The app now includes an optional Supabase collaboration mode for shared online editing.

Setup docs:
- `docs/supabase-collaboration.md`

Database schema:
- `supabase/schema.sql`
